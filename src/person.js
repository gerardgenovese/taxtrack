// const isAdult = (num) => {
//   return num >= 18 ? true : false;
// }

// const canDrink = (num) => {
//   return num >= 21 ? true : false;
// }

// export { isAdult, canDrink };


export default num => num > 65 ? false : true;